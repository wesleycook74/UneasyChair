$("#current_tab").html("<%= escape_javascript(render partial: 'tracks/show_accepted', locals: { track: @track} ) %>");

jQuery(document).ready( function($) {
    $(".clickable").click(function() {
        console.log($(this).data("link"));
        window.location = $(this).data("link");
    });
});