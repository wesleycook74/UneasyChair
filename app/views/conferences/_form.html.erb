<%= form_for(conference) do |f| %>
  <% if conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
      <% conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :acronym %>
    <%= f.text_field :acronym %>
  </div>

    <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :cols => 100, :rows => 25 %>
  </div>

  <div class="actions">
    <%= f.button :submit , :class => "btn btn-primary btn-sm active"%>
  </div>
<% end %>
